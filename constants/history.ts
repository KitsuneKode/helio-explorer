import { prefix } from '@/constants'

export const MAX_TOKENS = 30
export const MAX_WALLETS = 20
export const MAX_SWAP_ENTRIES = 50
export const HISTORY_STORAGE_KEY = `${prefix}-history`
export const SWAP_HISTORY_STORAGE_KEY = `${prefix}-swap-history`
